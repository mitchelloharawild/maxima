% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops.R
\name{Ops.mx_expr}
\alias{Ops.mx_expr}
\title{Arithmetic and comparison operators for Maxima expressions}
\usage{
\method{Ops}{mx_expr}(e1, e2)
}
\arguments{
\item{e1, e2}{<mx_expr> objects, or plain numbers.}
}
\value{
An <mx_expr> object.
}
\description{
Lets ordinary R arithmetic (\code{+}, \code{-}, \code{*}, \code{/}, \code{^}, \code{==}, \code{!=}, \code{<},
\code{>}, \code{<=}, \code{>=}) build up a Maxima expression: each operator maps
directly to the corresponding core Maxima form (see the package design
notes) and evaluates it, so e.g. \code{mx_symbol("x")^2 + 1} traces out the
expression tree the same way a plain R function does when called with
<mx_expr> arguments, the same technique used by other symbolic-math R
packages such as Ryacas and caracas. The other operand may be a plain R
number, converted with \code{\link[=as_mx_expr]{as_mx_expr()}}.
}
