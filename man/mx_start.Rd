% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boot.R
\name{mx_start}
\alias{mx_start}
\alias{mx_stop}
\title{Start or stop the embedded Maxima engine}
\usage{
mx_start()

mx_stop()
}
\value{
Invisibly, \code{NULL}.
}
\description{
\code{mx_start()} boots the vendored ECL runtime and loads the vendored
Maxima core into it. This happens lazily and automatically the first
time \code{\link[=mx_symbol]{mx_symbol()}} or \code{\link[=mx_call]{mx_call()}} is used, so most code never needs to
call it directly; it's exported for explicit control (e.g. to control
exactly when the (small, sub-second) startup cost is paid).
}
\details{
\code{mx_stop()} shuts the embedded ECL runtime down. This is one-way: ECL
does not support being re-booted after a shutdown in the same process,
so calling \code{\link[=mx_start]{mx_start()}} again afterwards will fail. For that reason
this package does not call \code{mx_stop()} automatically (e.g. when
unloaded); the embedded runtime is simply left running until the R
session itself exits.
}
