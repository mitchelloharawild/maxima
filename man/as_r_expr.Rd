% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expr.R
\name{as_r_expr}
\alias{as_r_expr}
\title{Convert a Maxima expression to an R expression}
\usage{
as_r_expr(x)
}
\arguments{
\item{x}{An <mx_expr> object.}
}
\value{
An R language object: a \code{call}, a symbol (\code{name}), or an
atomic value (\code{numeric}, \code{character}, \code{logical}, \code{complex}).
}
\description{
The mirror image of \code{\link[=as_mx_expr]{as_mx_expr()}}: converts an <mx_expr> into an
ordinary R language object (a \code{call}, a symbol, or an atomic value)
rather than a string, so the result can be inspected or manipulated
with R's own expression tools (\code{eval()}, \code{bquote()}, \code{all.vars()}, ...)
instead of just printed. Maxima's core arithmetic/relational forms
(\code{mplus}, \code{mtimes}, \code{mexpt}, ...) map back to the matching R operator
(the same fixed table \code{\link[=Ops.mx_expr]{Ops.mx_expr()}} uses to go the other way),
and Maxima's own list form becomes an R \code{list()} call; anything else
is an ordinary Maxima function call (including Maxima's own built-in
special functions, e.g. \code{sin}/\code{cos}/\code{log}), which maps to a same-named
R call the same generic way \code{\link[=mx_call]{mx_call()}} reaches it going the other
way. A small fixed set of core constants (\verb{\%e}, \verb{\%pi}, \verb{\%i}) and
booleans (\code{true}/\code{false}) map to their R equivalents; any other symbol
becomes a plain (lower-cased; Maxima itself is case-insensitive, see
\code{\link[=mx_symbol]{mx_symbol()}}) R symbol.
}
\examples{
\dontrun{
x <- mx_symbol("x")
as_r_expr(x^2 + 1)
eval(as_r_expr(mx_call("diff", x^2, x)), list(x = 3))
}
}
